<mvc:View
        xmlns:mvc="sap.ui.core.mvc"
        xmlns="sap.m"
        controllerName='nad.controller.NotFound'>
    <MessagePage
            title="{i18n>NotFound}"
            text="{i18n>NotFound.text}"
            description="{i18n>NotFound.description}"
            showNavButton="true"
            navButtonPress="onNavBack"/>
</mvc:View>